---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: pocket-id
  namespace: {{ .Release.Namespace }}
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`auth.home.daandewilde.be`)
      kind: Rule
      services:
        - name: pocketid-pocket-id
          port: 80
  tls: {}