sourcePVC: "bazarr-config"
destinationPVC: "temp-longhorn-pvc"

namespace: "paperless-ngx"
size: 2Gi

image: alpine:3.20

command:
  - /bin/sh
  - -c
  - |
    apk add --no-cache rsync &&
    echo "Starting migration..." &&
    rsync -aHAXv /source/ /dest/ &&
    echo "Done."