sourcePVC: "homarr-database"
destinationPVC: "homarr-database-lh"

sourceNamespace: "homarr"

image: alpine:3.20

command:
  - /bin/sh
  - -c
  - |
    apk add --no-cache rsync &&
    echo "Starting migration..." &&
    rsync -aHAXv /source/ /dest/ &&
    echo "Done."