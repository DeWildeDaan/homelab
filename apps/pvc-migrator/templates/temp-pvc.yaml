apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.tempPVCName }}
  namespace: {{ .Values.namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: {{ .Values.size }}