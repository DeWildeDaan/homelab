apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "printstash.fullname" . }}-data
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "printstash.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: {{ .Values.persistence.data.storageClass }}
  resources:
    requests:
      storage: {{ .Values.persistence.data.size }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "printstash.fullname" . }}-thumbs
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "printstash.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: {{ .Values.persistence.thumbs.storageClass }}
  resources:
    requests:
      storage: {{ .Values.persistence.thumbs.size }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "printstash.fullname" . }}-backups
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "printstash.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: {{ .Values.persistence.backups.storageClass }}
  resources:
    requests:
      storage: {{ .Values.persistence.backups.size }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "printstash.fullname" . }}-db
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "printstash.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .Values.persistence.db.storageClass }}
  resources:
    requests:
      storage: {{ .Values.persistence.db.size }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "printstash.fullname" . }}-staging
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "printstash.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .Values.persistence.staging.storageClass }}
  resources:
    requests:
      storage: {{ .Values.persistence.staging.size }}
